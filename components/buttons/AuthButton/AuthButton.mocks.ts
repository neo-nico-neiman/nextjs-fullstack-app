import { IAuthButton } from './AuthButton';

const base: IAuthButton = { 
    // add props
};

const alt: IAuthButton = {
    // add props
};

export const mockAuthButtonProps = {
    base,
    alt,
};