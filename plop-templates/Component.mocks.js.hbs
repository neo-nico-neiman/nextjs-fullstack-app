import { I{{pascalCase name}} } from './{{pascalCase name}}';

const base: I{{pascalCase name}} = { 
    // add props
};

const alt: I{{pascalCase name}} = {
    // add props
};

export const mock{{pascalCase name}}Props = {
    base,
    alt,
};